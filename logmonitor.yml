files:
  - file: /var/log/messages
    metric: messages
    enabled: true
    filter:
      - error
      - "perf:"
      - ".*took too long.*"
  
  - file: /var/log/syslog
    metric: syslog_systemd
    enabled: true
    filter:
      - "systemd\\[\\d+\\]"
